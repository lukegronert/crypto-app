import MainNav from '../components/MainNav';
export default function LandingPage({user}) {
    return (
        <section>
            <MainNav user={user} />
            LANDING PAGE
        </section>
    )
}